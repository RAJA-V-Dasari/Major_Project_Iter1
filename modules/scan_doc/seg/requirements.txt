# Requires Python 3.11 - the system Python 3.14 has no compatible
# torch/torchvision wheels. See README for venv setup.

# text detection (the detector that actually works on handwriting)
python-doctr[torch]

# fine-tuning + production inference
ultralytics

# pretrained baseline only; not used once a fine-tuned model exists
doclayout-yolo

opencv-python
numpy
pillow
